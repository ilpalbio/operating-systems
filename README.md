# Kernel Module Build Instructions

This project contains a simple Linux kernel module.  
Before compiling it, you must ensure that the required build tools are installed, including **make** and **gcc**.

---

## 📦 1. Install `make` and `gcc`

If your system does not have the necessary build tools, install them with:

```bash
sudo apt update
sudo apt install make gcc